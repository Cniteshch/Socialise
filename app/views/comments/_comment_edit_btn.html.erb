<% if comment.user_id == current_user.id %>
                    <%= link_to ' ', [@post, comment], :method => :delete,  data: {confirm: "You sure?"} , class: "btn btn-danger fa fa-trash-o btn_right" %>
                        <%= link_to ' ', [:edit, comment.post, comment], class: "btn btn-primary fa fa-edit btn_right "%>
<% else %>
    <% if @post.user_id == current_user.id %>
                        <%= link_to ' ', [@post, comment], :method => :delete,  data: {confirm: "You sure?"} , class: "btn btn-danger fa fa-trash-o btn_right" %>
    <% end %>
<% end %>